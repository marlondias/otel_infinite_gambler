apiVersion: 1

datasources:
  - name: InfiniteGambler_Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false

  - name: InfiniteGambler_Loki
    type: loki
    access: proxy
    url: http://loki:3100
    editable: false
